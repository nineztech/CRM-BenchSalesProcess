/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",                  // If you have an index.html file
    "./src/**/*.{js,jsx,ts,tsx}",    // Scans all files in the `src` folder with JS, JSX, TS, and TSX extensions
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
